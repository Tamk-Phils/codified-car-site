[build]
  command = "npm run build"
  publish = "dist/client"

[build.environment]
  NODE_VERSION = "20"

# Serve the SPA shell for all non-static routes
[[redirects]]
  from = "/*"
  to = "/_shell.html"
  status = 200
